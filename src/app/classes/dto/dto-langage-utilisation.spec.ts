import { DtoLangageUtilisation } from './dto-langage-utilisation';

describe('DtoLangageUtilisation', () => {
  it('should create an instance', () => {
    expect(new DtoLangageUtilisation()).toBeTruthy();
  });
});
