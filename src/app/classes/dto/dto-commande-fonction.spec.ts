import { DtoCommandeFonction } from './dto-commande-fonction';

describe('DtoCommandeFonction', () => {
  it('should create an instance', () => {
    expect(new DtoCommandeFonction()).toBeTruthy();
  });
});
